<h1>Home page</h1>
<% @posts.each do |post| %>
  <div class="alert alert-light">
  <h3> <%= post.title %></h3>
  <p> <%= post.body %></p>
    <%= link_to "Read more...", post_path(post), :class => 'btn btn-outline-primary' %>
  <br>
  </div>
<% end %>